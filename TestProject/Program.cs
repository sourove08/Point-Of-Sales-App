using System;
using System.Collections.Generic;
using System.Data.Entity;
using System.Data.Entity.Migrations;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using Microsoft.AspNet.Identity;
using POS.BAL;
using POS.Models;
using POS.Models.Database;
using POS.Models.Interfaces;

namespace TestProject
{
    class Program
    {
        static void Main(string[] args)
        {
        //    string passsword = "Abc001";
        //    var user = new ApplicationUser()
        //    {
        //        UserName = "Sourov",
        //        Email = "S@gmail.com"
        //    };
        //    ApplicationUserManager userManager=ApplicationUserManager.Create();
        //   IdentityResult result= userManager.Create(user,passsword);
        //    if (result.Succeeded)
        //    {
        //        Console.WriteLine("User Inserted Sucessfully");
        //    }
        //    else
        //    {
        //        foreach (var error in result.Errors)
        //        {
        //            Console.WriteLine(error);
        //        }
        //    }

        //    Console.ReadKey();
        }
    }
}
